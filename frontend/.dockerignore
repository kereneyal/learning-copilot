# Dependencies — reinstalled during image build
node_modules/

# Build output — regenerated by `next build` in builder stage
.next/

# Local env — must not leak into images
.env.local
.env*.local

# Dev tooling
.git/
.gitignore
*.sh
backups/

# Editor
.vscode/
.idea/
*.swp
